import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bridges.chat.pwa',
  appName: 'bridges-chat-pwa',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
